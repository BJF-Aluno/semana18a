import { render, screen } from '@testing-library/react';import App from './App';// Um teste para garantir que o texto "Projeto de CI/CD" está presentetest('renders CI/CD project title', () => {render(<App />);const titleElement = screen.getByText(/Projeto de CI/CD/i);expect(titleElement).toBeInTheDocument();});// Um teste para garantir que o texto do contador inicial está presentetest('renders initial count', () => {render(<App />);const countElement = screen.getByText(/Contador: 0/i);expect(countElement).toBeInTheDocument();});